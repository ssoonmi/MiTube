import React from 'react';
import {connect} from 'react-redux';
import HistoryPage from './history_page';

const msp = (state) => {
  return {

  };
};

const mdp = (dispatch) => {
  return {

  };
};

export default connect(msp, mdp)(HistoryPage);
